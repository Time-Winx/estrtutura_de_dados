Para a construção de uma boa estrutura utilizando hash, ela precisa ter velocidade e evitar colisões.
Onde para evitar colisões utiliza o método double hash que cria um maior espaçamento entre os nós.